# ITops Agent 深度修复方案（第二版）

> **生成时间**：2026-07-02
> **背景**：P0 安全修复 + P1 CI 门禁已完成，但后端"使用层"和前端工程化未跟进
> **核心目标**：让 98 个 eslint-disable 归零、764 处 any 降至 <100、前端建立 Service 层、告警链路统一

---

## 一、当前状态量化

| 指标 | 当前值 | 目标值 | 方案条目 |
|------|--------|--------|----------|
| 后端 `eslint-disable no-restricted-imports` | 98 个 / 91 文件 | ≤ 20 | R1-R6 |
| 后端 `any` 类型用法 | 764 处 / 100 文件 | < 100 | T1-T3 |
| 前端裸 `api.get/post` 调用 | 461 处 / 82 文件 | 0（全走 Service 层） | F1 |
| 前端 `any` 类型用法 | 339 处 / 70 文件 | < 50 | F2 |
| 前端 `eslint-disable` 文件 | 36 个 | 0 | F2 |
| 前端 >500 行组件 | ~20 个 | 0 | F3 |
| 前端 `useMutation` 无 `onError` | ~99/206 (48%) | < 10% | F4 |
| 前端独立 WebSocket 连接 | 6 套 | 1 套 | F5 |
| 前端测试文件 | 7 个（全冒烟） | ≥ 15 个（含业务测试） | F6 |
| 告警并行处理路径 | 4 条 | 1 条 | A1-A3 |
| `process.exit(0)` 定期重启 | 有 | 无（graceful shutdown） | S1 |
| AlertProcessor 测试 | 0 | ≥ 5 | A4 |

---

## 二、后端修复（R + T + A + S）

### R1: alerts 模块 Repository 迁移（优先，最复杂模块）

**目标**：将 alerts 模块下 15 个文件的 `import db` 迁移到 Repository 模式

**需迁移文件清单**（按 any 数量排序）：

| 文件 | any 数 | 迁移目标 Repository |
|------|--------|---------------------|
| `alerts/services/alertCorrelationService.ts` | 33 | `alertCorrelationRepository` |
| `alerts/services/alertAutoResponse/alertAutoResponseService.ts` | 16 | `alertAutoResponseRepository` |
| `alerts/services/alertAutoAnalyzer/analysisEngine.ts` | 7 | `alertAutoAnalysisRepository` |
| `alerts/services/alertAutoAnalyzer/deviceResolver.ts` | 11 | `alertDeviceResolverRepository` |
| `alerts/services/alertAutoAnalyzer/resultWriter.ts` | - | `alertAutoAnalysisRepository` |
| `alerts/services/alertAutoAnalyzer/alertFetcher.ts` | - | `alertRepository` |
| `alerts/services/alertAutoResponse/diagnosis/probeExecutor.ts` | 11 | `alertProbeRepository` |
| `alerts/services/alertAutoResponse/adaptive/deviceProfiler.ts` | 3 | `deviceProfilerRepository` |
| `alerts/services/alertAutoResponse/remediation/remediationExecutor.ts` | 7 | `alertRemediationLogRepository` |
| `alerts/services/alertAutoResponse/remediation/verificationGates.ts` | 6 | `alertVerificationRepository` |
| `alerts/services/alertAutoResponse/adaptive/adaptiveAutomation.ts` | 2 | `alertAutomationRepository` |
| `alerts/services/alertAutoResponse/adaptive/knowledgeFeedbackLoop.ts` | 1 | `alertKnowledgeRepository` |
| `alerts/services/alertAutoResponse/adaptive/strategyRecommender.ts` | 2 | `alertStrategyRepository` |
| `alerts/services/alertAutoResponse/adaptive/escalationEngine.ts` | 1 | `alertEscalationRepository` |
| `alerts/services/AlertProcessor.ts` | 6 | `alertProcessingRepository` |

**迁移步骤**（每个文件）：
1. 读取文件中所有 `db.prepare(...)` 调用
2. 在 `backend/src/repositories/` 下找到或新建对应 Repository 类
3. 将 SQL 操作移入 Repository 方法
4. 服务层改为通过 DI 容器或构造函数注入 Repository
5. 删除 `// eslint-disable-next-line no-restricted-imports`
6. 运行 `npx tsc --noEmit` + `npx vitest run` 验证

**验收标准**：
- [ ] alerts 模块下 0 个 `eslint-disable no-restricted-imports`
- [ ] alerts 模块下 `any` 数量 < 30
- [ ] 所有测试通过

---

### R2: ai 模块 Repository 迁移

**需迁移文件**：

| 文件 | any 数 | 迁移目标 |
|------|--------|----------|
| `ai/services/providers/extended.ts` | 35 | `aiProviderRepository` |
| `ai/services/remediation/aiRemediationService.ts` | 5 | `aiRemediationRepository` |
| `ai/services/rca/rootCauseAnalysisService/rcaAnalyzer.ts` | 5 | `rcaRepository`（已有） |
| `ai/services/knowledge/qanythingService.ts` | 6 | `knowledgeRepository` |
| `ai/services/agents/agentToolRegistry.ts` | 4 | `agentToolRepository` |
| `ai/services/llm/llmService/index.ts` | - | `llmConfigRepository` |
| `ai/services/llm/llmService/providerAdapters.ts` | - | `llmProviderRepository` |
| `ai/services/llm/llmService/circuitBreaker.ts` | 1 | 保持（无 DB 访问） |
| `ai/services/llm/llmService/toolCalling.ts` | 1 | `toolCallRepository` |
| `ai/services/multiAgent/Coordinator.ts` | 2 | 无 DB（仅 any 修复） |

**验收标准**：
- [ ] ai 模块下 0 个 `eslint-disable no-restricted-imports`
- [ ] ai 模块下 `any` 数量 < 15

---

### R3: network 模块 Repository 迁移

**需迁移文件**：

| 文件 | any 数 | 迁移目标 |
|------|--------|----------|
| `network/services/networkDiscoveryService.ts` | 13 | `networkDiscoveryRepository` |
| `network/services/networkDeviceService.ts` | 6 | `networkDeviceRepository`（已有） |
| `network/services/snmpService.ts` | 11 | `snmpRepository` |
| `network/services/snmpTrapService.ts` | 10 | `snmpTrapRepository` |
| `network/services/lldpDiscoveryService.ts` | 5 | `lldpRepository` |
| `network/services/snmpPollingService.ts` | 3 | `snmpPollingRepository` |
| `network/services/networkInspectionService.ts` | 1 | `networkInspectionRepository` |
| `network/services/topologyService.ts` | - | `topologyRepository` |
| `network/services/networkCommandGenerator.ts` | 1 | 无 DB（仅 any 修复） |

---

### R4: containers 模块 Repository 迁移

| 文件 | any 数 | 迁移目标 |
|------|--------|----------|
| `containers/services/dockerService.ts` | 21 | `containerRepository` |
| `containers/routes/containerRoutes.ts` | 40 | 改为调用 Service |
| `containers/routes/virtualMachineRoutes.ts` | 32 | 改为调用 Service |
| `containers/routes/volumeRoutes.ts` | 8 | 改为调用 Service |
| `containers/services/multiHostDockerService.ts` | 11 | `multiHostRepository` |
| `containers/services/vmManagement/index.ts` | 8 | `vmManagementRepository` |
| `containers/services/registryService.ts` | 13 | `registryRepository`（已有） |
| `containers/services/vmSnapshotSchedulerService.ts` | 6 | `vmSnapshotRepository` |
| `containers/services/vmMigrationService.ts` | 5 | `vmMigrationRepository` |
| `containers/services/containerLogService.ts` | 7 | `containerLogRepository` |

---

### R5: infra + auto + kubernetes + workflow 模块迁移

| 模块 | 文件数 | any 总数 | 迁移目标 |
|------|--------|----------|----------|
| infra | 12 | ~40 | `infraRepository`, `configRepository` 等 |
| auto | 6 | ~55 | `remediationPolicyRepository`, `autoScaleRepository` |
| kubernetes | 1 | 29 | `kubernetesRepository` |
| workflow | 8 | ~15 | `workflowRepository`, `schedulerRepository` |
| monitor | 3 | ~10 | `monitorRepository`, `costRepository` |
| servers | 4 | ~5 | `serverRepository`（已有） |
| dc | 1 | 5 | `dcRepository` |
| auth | 3 | ~5 | `userRepository`（已有） |

---

### R6: 升级 no-restricted-imports 为 error

在 R1-R5 完成后：
1. 删除 `backend/.eslintrc.json` 中 `no-restricted-imports` 的 `warn` → 改为 `error`
2. 删除 `ignorePatterns` 中所有临时豁免
3. 运行 `npx eslint src/ --rule '{"no-restricted-imports": "error"}'` 确认 0 违规

**验收标准**：
- [ ] 后端 `eslint-disable no-restricted-imports` = 0
- [ ] ESLint error 级别强制执行

---

### T1: alerts 模块 any 清理

在 R1 迁移完成后，清理剩余 `any`：

**重灾区文件**：
- [alertCorrelationService.ts](file:///f:/自开发代码/多Agent运维平台/ITops%20agent/daima/backend/src/modules/alerts/services/alertCorrelationService.ts) — 33 处
  - `:168 calculateCorrelationScore(a: any, b: any)` → 定义 `CorrelationInput` 接口
  - `:216 matchAlertToGroup(alert: any, ...)` → 使用 `Alert` 接口
  - `:288 addToGroup(..., alert: any)` → 同上
  - `:526 getStats(): any` → 定义 `CorrelationStats` 接口
- [AlertProcessor.ts](file:///f:/自开发代码/多Agent运维平台/ITops%20agent/daima/backend/src/modules/alerts/services/AlertProcessor.ts) — 6 处
  - `:66 catch (err: any)` → `catch (err: unknown)`
  - `:122 catch (err: any)` → 同上
  - `:228 getOrCreatePolicy(): Promise<any>` → `Promise<RemediationPolicy>`
  - `:236 .get(...) as any` → 使用正确类型
  - `:311 getRecordByAlertId(): any` → `ProcessingRecord | null`

---

### T2: ai + containers 模块 any 清理

- [extended.ts](file:///f:/自开发代码/多Agent运维平台/ITops%20agent/daima/backend/src/modules/ai/services/providers/extended.ts) — 35 处 → 定义 provider 接口
- [executionOrchestration.ts](file:///f:/自开发代码/多Agent运维平台/ITops%20agent/daima/backend/src/modules/auto/services/remediationService/executionOrchestration.ts) — 20 处 → 定义执行上下文接口
- [verificationRollback.ts](file:///f:/自开发代码/多Agent运维平台/ITops%20agent/daima/backend/src/modules/auto/services/remediationService/verificationRollback.ts) — 18 处 → 定义验证结果接口
- [dockerService.ts](file:///f:/自开发代码/多Agent运维平台/ITops%20agent/daima/backend/src/modules/containers/services/dockerService.ts) — 21 处 → 定义 Docker API 响应接口
- [kubernetesService.ts](file:///f:/自开发代码/多Agent运维平台/ITops%20agent/daima/backend/src/modules/kubernetes/services/kubernetesService.ts) — 29 处 → 定义 K8s 资源接口
- [containerRoutes.ts](file:///f:/自开发代码/多Agent运维平台/ITops%20agent/daima/backend/src/modules/containers/routes/containerRoutes.ts) — 40 处 → 使用 Service 层类型

---

### T3: 其余模块 any 清理

- `catch (err: any)` 全局替换为 `catch (err: unknown)` + `err instanceof Error ? err.message : String(err)`
- `as any` → 定义正确接口或使用 `unknown` + 类型守卫
- `: any` 参数 → 定义具体类型

**验收标准**：
- [ ] 后端 `any` 总量 < 100
- [ ] `catch (err: any)` = 0
- [ ] `@typescript-eslint/no-explicit-any` 升为 `error`

---

### A1: 告警链路统一 — 删除独立轮询

**问题**：4 条并行路径
1. AlertProcessor `processAlert`（统一入口）
2. alertAutoResponseService 独立 `processAlert`
3. alertAutoAnalyzer 每 15s 独立轮询
4. alertService `setImmediate` 异步 RCA

**修复步骤**：

1. **删除 alertAutoAnalyzer 独立轮询**：
   - [alertAutoAnalyzer/index.ts:81](file:///f:/自开发代码/多Agent运维平台/ITops%20agent/daima/backend/src/modules/alerts/services/alertAutoAnalyzer/index.ts#L81) 删除 `setInterval` 轮询
   - 改为 `export async function analyzeSingleAlert(alertId: string)` 供 AlertProcessor 调用
   - 在 `serviceRegistry.ts` 中停止调用 `alertAutoAnalyzer.start()`

2. **删除 alertAutoResponseService 遗留轮询**：
   - [alertAutoResponseService.ts:64-80](file:///f:/自开发代码/多Agent运维平台/ITops%20agent/daima/backend/src/modules/alerts/services/alertAutoResponse/alertAutoResponseService.ts#L64) 删除 `isAlreadyProcessedByAnalyzer` 及相关代码
   - 删除注释为"已由 AlertProcessor 统一接管，保留供参考"的 `fetchPendingAlerts`

3. **AlertProcessor 成为唯一入口**：
   - 所有告警触发（webhook、手动创建、定时检查）都经过 `AlertProcessor.processAlert()`
   - AlertProcessor 内部调用 AARS `triggerManually` 和 RCA `autoAnalyze`
   - 去重逻辑集中在 AlertProcessor 的 `processingIds` Set

**验收标准**：
- [ ] 仅 1 个告警入口（AlertProcessor）
- [ ] 0 个独立 `setInterval` 轮询告警
- [ ] 去重逻辑集中在一处

---

### A2: 告警链路统一 — 状态机

**修复步骤**：

1. 创建 `backend/src/types/alert-state-machine.ts`：
```typescript
export type AlertState = 'pending' | 'processing' | 'analyzing' | 'remediating' | 'verifying' | 'resolved' | 'failed';
export type AlertEvent = 'TRIGGER' | 'ANALYZE' | 'REMEDIATE' | 'VERIFY' | 'SUCCESS' | 'FAIL' | 'TIMEOUT';
export const AlertStateMachine = { /* 状态转换表 */ };
```

2. 将 `alert_processing_records` 表作为唯一状态源
3. AlertProcessor 每次状态转换时更新记录
4. AARS 和 RCA 通过查询记录判断当前状态

**验收标准**：
- [ ] 1 张表作为告警状态唯一源
- [ ] 状态转换有日志记录

---

### A3: 告警链路统一 — 删除重复类型和桩实现

- 删除 `alertNotificationService.ts:7-31` 重复的 `AlertLevel`/`AlertChannel` 类型
- 统一使用 `types/unified-alert-processing.ts` 中的定义
- 删除 `alertProviderRegistry.ts:89-127` 桩实现（返回 `[]` 的 provider）
- 统一使用 `alertSourceAdapters.ts` 的适配器

---

### A4: AlertProcessor 测试

**修复步骤**：

创建 `backend/src/modules/alerts/services/AlertProcessor.test.ts`：
```typescript
describe('AlertProcessor', () => {
  it('processAlert handles critical severity with hybrid strategy');
  it('processAlert handles medium severity with aars strategy');
  it('processAlert deduplicates by alertId');
  it('processAlert handles AARS failure gracefully');
  it('processAlert handles workflow failure gracefully');
  it('makeDecision uses knowledge base match when successRate >= threshold');
  it('makeDecision falls back to severity-based strategy');
});
```

**验收标准**：
- [ ] AlertProcessor 测试 ≥ 5 个
- [ ] 覆盖所有策略路径（aars/workflow/hybrid/auto）
- [ ] 覆盖去重逻辑

---

### S1: Graceful Shutdown + 移除 process.exit

**问题**：
- [app.ts:68-78](file:///f:/自开发代码/多Agent运维平台/ITops%20agent/daima/backend/src/app.ts#L68) 每天 3 点 `process.exit(0)` 掩盖内存泄漏
- [handler.ts:212-218](file:///f:/自开发代码/多Agent运维平台/ITops%20agent/daima/backend/src/shared/websocket/handler.ts#L212) SIGTERM/SIGINT 只 log 不关闭
- `shutdownAllServices()` 定义但从未调用

**修复步骤**：

1. 修改 [app.ts](file:///f:/自开发代码/多Agent运维平台/ITops%20agent/daima/backend/src/app.ts)：
```typescript
// 删除 L68-78 的 setTimeout process.exit(0)

// 添加 graceful shutdown
const shutdown = async (signal: string) => {
  console.log(`收到 ${signal}，开始优雅关闭...`);
  httpServer.close();
  io.close();
  await shutdownAllServices();
  process.exit(0);
};

process.on('SIGTERM', () => shutdown('SIGTERM'));
process.on('SIGINT', () => shutdown('SIGINT'));
```

2. 修改 [handler.ts:212-218](file:///f:/自开发代码/多Agent运维平台/ITops%20agent/daima/backend/src/shared/websocket/handler.ts#L212)：
```typescript
// 删除原有的空 log 处理器
// graceful shutdown 由 app.ts 统一管理
```

3. 确保 `shutdownAllServices()` 调用所有注册的 shutdown 钩子

**验收标准**：
- [ ] 无 `process.exit(0)` 定期重启
- [ ] SIGTERM/SIGINT 触发 `shutdownAllServices()`
- [ ] 所有 `schedulerService.stop()`、`alertCorrelationService.stop()` 等被调用

---

### S2: multiAgent 接入启动流程

**修复步骤**：

1. 在 [serviceRegistry.ts](file:///f:/自开发代码/多Agent运维平台/ITops%20agent/daima/backend/src/serviceRegistry.ts) 注册 multiAgent：
```typescript
container.register('multiAgentSystem', () => {
  initializeMultiAgentSystem();
  return { name: 'multiAgentSystem' };
}, []);
```

2. 删除 [multiAgentRoutes.ts:20](file:///f:/自开发代码/多Agent运维平台/ITops%20agent/daima/backend/src/modules/ai/routes/multiAgentRoutes.ts#L20) 的懒初始化

3. 精简 [Specialists.ts](file:///f:/自开发代码/多Agent运维平台/ITops%20agent/daima/backend/src/modules/ai/services/multiAgent/Specialists.ts) 中 8 个复制粘贴的 Specialist — 提取公共 `executeWithLLM` 基类方法

---

## 三、前端修复（F1-F6）

### F1: 建立统一 Service 层（最高优先）

**问题**：461 处 `api.get/post/put/delete` 散落在 82 个组件中

**修复步骤**：

1. 为每个模块创建 `api.ts`（参照已有的 [mcp/api.ts](file:///f:/自开发代码/多Agent运维平台/ITops%20agent/daima/frontend/src/modules/mcp/api.ts)）：

```
frontend/src/modules/alerts/api.ts      ← 15 个 API 函数
frontend/src/modules/ai/api.ts           ← 11 个 API 函数
frontend/src/modules/containers/api.ts  ← 17 个 API 函数
frontend/src/modules/infra/api.ts       ← 20 个 API 函数
frontend/src/modules/network/api.ts     ← 10 个 API 函数
frontend/src/modules/servers/api.ts      ← 20 个 API 函数
frontend/src/modules/workflow/api.ts    ← 7 个 API 函数
frontend/src/modules/monitor/api.ts     ← 9 个 API 函数
frontend/src/modules/auth/api.ts        ← 4 个 API 函数
frontend/src/modules/dc/api.ts          ← 43 个 API 函数
frontend/src/modules/auto/api.ts        ← 7 个 API 函数
frontend/src/modules/database/api.ts    ← 6 个 API 函数
frontend/src/modules/kubernetes/api.ts  ← 17 个 API 函数
```

2. 每个 Service 文件结构（参照 mcp/api.ts）：
```typescript
import api from '@/lib/api';
import type { Alert } from './types';

export const alertApi = {
  list: (params?: AlertListParams) =>
    api.get<ApiResponse<Alert[]>>('/api/alerts', { params }),
  getById: (id: string) =>
    api.get<ApiResponse<Alert>>(`/api/alerts/${id}`),
  acknowledge: (id: string) =>
    api.post<ApiResponse<void>>(`/api/alerts/${id}/acknowledge`),
};
```

3. 逐模块将组件内的 `api.get(...)` 替换为 Service 调用

4. 在 [lib/api.ts](file:///f:/自开发代码/多Agent运维平台/ITops%20agent/daima/frontend/src/lib/api.ts) 增加统一响应提取：
```typescript
api.interceptors.response.use(
  (response) => {
    if (response.data?.success !== undefined) {
      response.data = response.data.data;
    }
    return response;
  },
  (error) => Promise.reject(new ApiError(error.response?.data?.message || '网络错误'))
);
```

**验收标准**：
- [ ] 每个模块有 `api.ts` Service 文件
- [ ] 组件中 0 处直接 `api.get/post` 调用
- [ ] API 响应有统一类型和错误处理

---

### F2: 类型安全治理

**修复步骤**：

1. 创建共享类型定义文件：
```
frontend/src/types/
├── alert.ts      ← Alert, AlertLevel, AlertStats
├── server.ts     ← Server, ServerGroup
├── container.ts  ← Container, Image, Volume, VM
├── workflow.ts   ← Workflow, Task, Node
├── ai.ts         ← AIModel, Agent, RCA
├── network.ts    ← NetworkDevice, SnmpCredential
└── api.ts        ← ApiResponse<T>, PaginatedResponse<T>
```

2. 删除 36 个文件的 `/* eslint-disable @typescript-eslint/no-explicit-any */`

3. 逐文件将 `any` 替换为共享类型

**重灾区文件**：
- [DataCenterManage/index.tsx](file:///f:/自开发代码/多Agent运维平台/ITops%20agent/daima/frontend/src/modules/dc/pages/DataCenterManage/index.tsx) — 36 处 any
- [WorkflowEditor.tsx](file:///f:/自开发代码/多Agent运维平台/ITops%20agent/daima/frontend/src/modules/workflow/pages/WorkflowEditor.tsx) — 28 处
- [NetworkDevices.tsx](file:///f:/自开发代码/多Agent运维平台/ITops%20agent/daima/frontend/src/modules/network/pages/NetworkDevices.tsx) — 15 处
- [useServerActions.ts](file:///f:/自开发代码/多Agent运维平台/ITops%20agent/daima/frontend/src/modules/servers/pages/useServerActions.ts) — 14 处
- [VirtualMachines.tsx](file:///f:/自开发代码/多Agent运维平台/ITops%20agent/daima/frontend/src/modules/containers/pages/VirtualMachines.tsx) — 13 处

**验收标准**：
- [ ] `any` 数量 < 50
- [ ] `eslint-disable` 文件数 = 0
- [ ] `@typescript-eslint/no-explicit-any` 升为 `error`

---

### F3: 超大组件拆分

**优先拆分清单**：

| 文件 | 行数 | useState | 拆分方案 |
|------|------|----------|----------|
| [VirtualMachines.tsx](file:///f:/自开发代码/多Agent运维平台/ITops%20agent/daima/frontend/src/modules/containers/pages/VirtualMachines.tsx) | ~1500 | 22 | VMList / VMCreate / VMDetail |
| [BigScreenDashboard.tsx](file:///f:/自开发代码/多Agent运维平台/ITops%20agent/daima/frontend/src/modules/monitor/pages/BigScreenDashboard.tsx) | 1299 | 11 | 多个 ChartCard 组件 |
| [WorkflowEditor.tsx](file:///f:/自开发代码/多Agent运维平台/ITops%20agent/daima/frontend/src/modules/workflow/pages/WorkflowEditor.tsx) | >1200 | 9 | Canvas / NodePanel / EdgePanel / Toolbar |
| [Containers.tsx](file:///f:/自开发代码/多Agent运维平台/ITops%20agent/daima/frontend/src/modules/containers/pages/Containers.tsx) | 1071 | 39 | ContainerList / CreateForm / NetworkForm |
| [Kubernetes.tsx](file:///f:/自开发代码/多Agent运维平台/ITops%20agent/daima/frontend/src/modules/kubernetes/pages/Kubernetes.tsx) | ~1064 | 16 | NodeList / PodList / ServiceList |
| [DataCenterManage/index.tsx](file:///f:/自开发代码/多Agent运维平台/ITops%20agent/daima/frontend/src/modules/dc/pages/DataCenterManage/index.tsx) | 961 | - | 拆分 Tab 组件 |
| [useDataCenter.ts](file:///f:/自开发代码/多Agent运维平台/ITops%20agent/daima/frontend/src/modules/dc/pages/DataCenterManage/useDataCenter.ts) | >500 | 56 | 拆为 useRooms / useRacks / useDevices / useEnv |
| [Tasks.tsx](file:///f:/自开发代码/多Agent运维平台/ITops%20agent/daima/frontend/src/modules/workflow/pages/Tasks.tsx) | ~860 | 6 | TaskList / TaskDetail / TaskForm |

**拆分原则**：
- 每个拆分后文件 ≤ 400 行
- 每个组件 ≤ 5 个 useState
- 跨组件状态通过 zustand store 管理

**验收标准**：
- [ ] 0 个超过 500 行的组件文件
- [ ] 单组件 useState ≤ 5 个

---

### F4: 错误处理标准化

**问题**：206 个 `useMutation` 中仅 107 个有 `onError`（48% 缺失）

**修复步骤**：

1. 创建 `frontend/src/hooks/useApiError.ts`：
```typescript
export function useApiError() {
  const { showToast } = useToast();
  return {
    handleError: useCallback((error: unknown) => {
      const message = error instanceof ApiError ? error.message : '未知错误';
      showToast({ type: 'error', message });
    }, [showToast]),
  };
}
```

2. 为所有无 `onError` 的 `useMutation` 添加：
```typescript
const { handleError } = useApiError();
const { mutate } = useMutation({
  mutationFn: alertApi.acknowledge,
  onError: handleError,
});
```

3. 修复 `ChatWidget.tsx` catch 返回假数据

**重灾区**：
- `useServerActions.ts` — 14 个 mutation，仅 3 个有 onError
- `Tasks.tsx` — 6 个 mutation，0 个 onError

**验收标准**：
- [ ] 所有 `useMutation` 有 `onError`
- [ ] 无 catch 返回假数据模式
- [ ] 无 `useQuery` 未解构 `error`

---

### F5: WebSocket 统一

**问题**：6 套独立 WebSocket 连接

**修复步骤**：

1. 重写 [useSocketIO.ts](file:///f:/自开发代码/多Agent运维平台/ITops%20agent/daima/frontend/src/lib/useSocketIO.ts) 为通用 Hook：
```typescript
interface UseSocketIOOptions {
  room?: string;
  events: string[];
  onEvent: (event: string, data: unknown) => void;
}
export function useSocketIO(options: UseSocketIOOptions) { ... }
```

2. 创建 `contexts/SocketContext.tsx` 全局单例

3. 迁移 6 个独立连接：
- `ContainerLogs.tsx:37` → 改用 `useSocketIO`
- `ContainerMonitor.tsx:57` → 改用
- `WebTerminal.tsx:79` → 改用
- `RemoteDesktop.tsx:98` → 保持（跨域连接）
- `Tasks.tsx:105` → 改用
- `Alerts.tsx:96` → 改用

**验收标准**：
- [ ] 全局仅 1 个 Socket.IO 连接（RemoteDesktop 除外）
- [ ] `useSocketIO` 可复用于任意场景

---

### F6: 前端测试

**修复步骤**：

1. 为核心组件编写渲染测试（使用已安装的 `@testing-library/react`）：
- `Login.test.tsx` — 表单渲染、输入验证、提交
- `Alerts.test.tsx` — 列表渲染、筛选
- `Servers.test.tsx` — 列表渲染、添加表单
- `Settings.test.tsx` — 各 Tab 渲染

2. 为 Service 层编写 API mock 测试：
- `alertApi.test.ts` — 验证参数和响应处理
- `serverApi.test.ts` — 同上

3. 为路由编写导航测试

**验收标准**：
- [ ] ≥ 15 个前端测试文件
- [ ] 测试使用 `@testing-library/react` 渲染组件
- [ ] 前端覆盖率 ≥ 15%

---

## 四、执行顺序

```
阶段 1（后端告警链路，最高风险）
├─ A1 删除独立轮询
├─ A2 状态机
├─ A3 删除重复类型
├─ A4 AlertProcessor 测试
└─ S1 Graceful Shutdown

阶段 2（后端 Repository 迁移）
├─ R1 alerts 模块
├─ R2 ai 模块
├─ R3 network 模块
├─ R4 containers 模块
├─ R5 其余模块
└─ R6 升级为 error

阶段 3（后端类型清理）
├─ T1 alerts any 清理
├─ T2 ai + containers any 清理
└─ T3 其余模块 any 清理

阶段 4（前端工程化）
├─ F1 Service 层（13 模块）
├─ F2 类型安全
├─ F3 组件拆分
├─ F4 错误处理
├─ F5 WebSocket 统一
└─ F6 测试

阶段 5（收尾）
├─ S2 multiAgent 接入
└─ 最终验证
```

**关键依赖**：
- R1 必须在 T1 之前（先迁移 Repository 再清 any）
- F1 必须在 F2 之前（先建 Service 层再清类型）
- A1-A4 可以并行（告警链路修改不依赖 Repository 迁移）

---

## 五、验收清单

### 后端
- [ ] `eslint-disable no-restricted-imports` = 0
- [ ] `any` 总量 < 100
- [ ] `catch (err: any)` = 0
- [ ] 告警处理仅 1 条入口路径
- [ ] AlertProcessor 测试 ≥ 5 个
- [ ] 无 `process.exit(0)` 定期重启
- [ ] SIGTERM/SIGINT 触发 graceful shutdown
- [ ] multiAgent 在启动时初始化
- [ ] TypeScript 编译通过
- [ ] ESLint 零 error
- [ ] 全部测试通过

### 前端
- [ ] 每个模块有 `api.ts` Service 文件
- [ ] 组件中 0 处直接 `api.get/post`
- [ ] `any` 数量 < 50
- [ ] `eslint-disable` 文件数 = 0
- [ ] 0 个超过 500 行的组件
- [ ] 所有 `useMutation` 有 `onError`
- [ ] 全局仅 1 个 Socket.IO 连接
- [ ] ≥ 15 个前端测试文件
- [ ] TypeScript 编译通过
- [ ] ESLint 零 error
