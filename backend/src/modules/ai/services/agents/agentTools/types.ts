/**
 * Agent 工具子模块共享类型（2026-07-21 拆分）
 *
 * AgentTool / AgentToolRiskLevel 类型仍由 ../agentToolRegistry.ts 定义（保持外部 import 兼容）
 * 本文件用于工具分类文件共享的工具输入参数类型。
 *
 * 注：当前所有工具分类文件未用到 ToolInputArgs（使用 `Record<string, unknown>` 内联更直观），
 *     此文件作为未来类型扩展的占位。
 */
export {}; // 占位，确保文件被识别为模块
